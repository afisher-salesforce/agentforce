{
  "name": "siemens-agentforce-heroku",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "heroku-postbuild": "echo 'Pre-built dist/public is committed to repo — no build step needed' && ls -la dist/public/",
    "start": "node server.js"
  },
  "dependencies": {}
}
