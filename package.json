{
  "name": "siemens-agentforce-heroku",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "heroku-postbuild": "echo 'Using pre-built dist/public from repo' && ls -la dist/public/ && ls -la dist/public/assets/",
    "start": "node server.js"
  },
  "dependencies": {}
}
