{
  "name": "siemens-agentforce-heroku",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd artifacts/agentforce-workshop && npm install && npm run build",
    "heroku-postbuild": "npm run build",
    "start": "node artifacts/agentforce-workshop/server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
